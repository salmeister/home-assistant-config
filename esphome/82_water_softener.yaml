substitutions:
  name: water_softener
  display_name: "Water Softener Salt Level"
  id: "82"

<<: !include base.yaml

esphome:
  name: ${name}
  platform: ESP8266
  board: d1_mini_lite

sensor:
  - platform: ultrasonic
    trigger_pin: D1
    echo_pin: D2
    unit_of_measurement: "%"
    icon: "mdi:cup-water"
    accuracy_decimals: 0
    name: ${display_name}
    update_interval: 60s
  
    filters:
      - lambda: return ((85-((x*100)-9))/85)*100;
      - filter_out: nan
